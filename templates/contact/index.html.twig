{% extends 'base.html.twig' %}

{% block body %}
<table class="table table-dark">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Nom</th>
      <th scope="col">Prenom</th>
      <th scope="col">Email</th>
      <th scope="col">Telephone</th>
    </tr>
  </thead>
  <tbody>
  {% for detail in contact %}
    <tr>
  <th scope="row"> {{ detail.id }} </th>
  <td> {{ detail.nom}} </td>
  <td> {{ detail.prenom}} </td>
  <td> {{ detail.email}} </td>
  <td> {{ detail.telephone}} </td>
  <td><a class="btn btn-secondary" type="button" href="{{path('contact_details', {id: detail.id})}}"><i class="fa fa-search"></i></a></td>
  <td><a class="btn btn-secondary" type="button" href="{{path('contact_delete', {id: detail.id})}}"><i class="bi bi-trash"></i></td>
    <td><a class="btn btn-secondary" type="button" href="{{path('contact_edit', {id: detail.id})}}"><i class="bi bi-pencil"></i></td>
</a><td>
    </tr>
 {% endfor %}
  </tbody>
</table>

<br>
<br>
<div><a class="btn btn-secondary" type="button" href="{{path('contact_add')}}">Ajouter contact</div>
<div><a class="btn btn-secondary" type="button" href="{{path('app_register')}}">creer compte</div>
<div><a class="btn btn-secondary" type="button" href="{{path('app_login')}}">Se connecter</div>
{% endblock %}